let numero = 5; // Você pode alterar o valor de 'numero' para gerar a tabuada de outro número

/*Gerar a tabuada do número especificado*/
for (let i = 1; i <= 10; i++) {
  console.log(`${numero} x ${i} = ${numero * i}`);
}